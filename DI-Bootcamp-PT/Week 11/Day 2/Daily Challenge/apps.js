// apps.js
import { generateColorfulMessage } from "./colorful-message.js";

// Call the function to get the colorful message
const message = generateColorfulMessage();

// Display the colorful message in the terminal
console.log(message);
